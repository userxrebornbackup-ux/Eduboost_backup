API Routers
===========

The ``app.api_v2_routers`` package contains thin FastAPI router modules —
one per domain.  Business logic lives in ``app.modules``; routers only handle
HTTP concerns (request parsing, response serialisation, dependency injection).

.. automodule:: app.api_v2_routers
   :members:
   :undoc-members:
   :show-inheritance:
